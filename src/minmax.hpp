#ifndef MINMAX_HPP
#define MINMAX_HPP

#include <vector>

double minmax1(const std::vector<int> vetor);
double minmax2(const std::vector<int> vetor);
double minmax3(const std::vector<int> vetor);
void trabalho();

#endif
